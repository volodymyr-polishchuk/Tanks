apply plugin: 'java'
apply plugin: 'application'

application {
    mainClass = 'com.vpolishchuk.tanks.launcher.Launcher'
}

jar {
    archiveVersion = '0.0.1'
    archivesBaseName = 'tanks'

    manifest {
        attributes(
                'Main-Class': 'com.vpolishchuk.tanks.launcher.Launcher'
        )
    }
}
